galaxy_info:
  role_name: bitwarden
  author: Andrew Arensburger
  description: Install and update Bitwarden desktop application.
  company: Ooblick.com

  # If the issue tracker for your role is not on github, uncomment the
  # next line and provide a value
  # issue_tracker_url: http://example.com/issue/tracker

  # Choose a valid license ID from https://spdx.org - some suggested licenses:
  # - BSD-3-Clause (default)
  # - MIT
  # - GPL-2.0-or-later
  # - GPL-3.0-only
  # - Apache-2.0
  # - CC-BY-4.0
  license: GPL-2.0-or-later

  # >= 2.5 for "version()" (rather than version_compare())
  min_ansible_version: 2.5

  # If this a Container Enabled role, provide the minimum Ansible Container version.
  # min_ansible_container_version:

  # To view available platforms and versions (or releases), visit:
  # https://galaxy.ansible.com/api/v1/platforms/

  # Hopefully this is fairly portable, but these are the platforms on
  # which this has been tested.
  platforms:
    - name: Ubuntu
      versions:
        # - all
        - focal
    # - name: MacOSX
    #   versions:
    #     - 11.4

  galaxy_tags:
    # List tags for your role here, one per line. A tag is a keyword that describes
    # and categorizes the role. Users find roles by searching for tags. Be sure to
    # remove the '[]' above, if you add tags to this list.
    #
    # NOTE: A tag is limited to a single word comprised of alphanumeric characters.
    #       Maximum 20 tags per role.
    - bitwarden
    - desktop

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
